info:
  name: Gamification
  type: http
  seq: 16

http:
  method: GET
  url: /api/account/gamification
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
