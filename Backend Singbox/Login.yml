info:
  name: Login
  type: http
  seq: 5

http:
  method: POST
  url: https://singbox-backend.onrender.com/api/login
  body:
    type: json
    data: |-
      {
        "email": "contactsingbox@gmail.com",
        "password": "82d0d6d8c2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
