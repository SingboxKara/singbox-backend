info:
  name: Login
  type: http
  seq: 5

http:
  method: POST
  url: https://singbox-backend.onrender.com/api/login
  body:
    type: json
    data: |-
      {
        "email": "testsingbox123@example.com",
        "password": "test1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
