info:
  name: Reservation Game
  type: http
  seq: 17

http:
  method: GET
  url: /api/admin/mark-reservation-completed
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
