info:
  name: Register
  type: http
  seq: 4

http:
  method: POST
  url: https://singbox-backend.onrender.com/api/register
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
