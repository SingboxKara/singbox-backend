info:
  name: Verify cart
  type: http
  seq: 8

http:
  method: POST
  url: https://singbox-backend.onrender.com/api/verify-cart
  body:
    type: json
    data: |-
      {
        "items": [
          {
            "date": "2026-04-10",
            "hour": 18,
            "boxId": 1,
            "persons": 4
          }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
